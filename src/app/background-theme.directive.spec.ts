import { BackgroundThemeDirective } from './background-theme.directive';

describe('BackgroundThemeDirective', () => {
  it('should create an instance', () => {
    const directive = new BackgroundThemeDirective();
    expect(directive).toBeTruthy();
  });
});
